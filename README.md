# Neural-Network-Classifier
A neural network classifier built from scratch using NumPy. Implements forward and backward propagation with support for multiple layers and activation functions. Includes dataset preprocessing, training with gradient descent, and model evaluation metrics.
